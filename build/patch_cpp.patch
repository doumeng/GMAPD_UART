--- ../task_module/cpp/cooler_control.cpp
+++ ../task_module/cpp/cooler_control.cpp
@@ -237,5 +237,14 @@
         }).detach();
     }
 
+    void saveConfigAsync(CoolerCallback callback) {
+        if (!callback) {
+            return;
+        }
+        std::thread([callback]() {
+            uint8_t result = saveConfig();
+            bool success = (result == 0);
+            callback(success, result);
+        }).detach();
+    }
 } // namespace Cooler
-
